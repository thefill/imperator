// import {processorService} from './processor.service';

describe('Processor service', () => {
    it.skip('should be created', () => {
        // expect(typeof processorService).not.toEqual('function');
    });
});
